config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 5
scenarios:
  - name: health
    flow:
      - get:
          url: /health/live
      - get:
          url: /metrics
